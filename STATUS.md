# Release status

Current status: **Candidate — `E2E` tutorial source complete, no execution of this revision recorded**. The pipeline package with its adaptation contract (`adapt`, `evaluate`, `save_artifact`, `load_artifact` / `from_artifact`), the carried `metrics.py` and `samples.py` (the BLIP captioning row's digest-pinned VizWiz-Captions sample), the offline unit suites (pipeline, adaptation, role-helper, import-boundary and notebook-parity) and the model-backed adaptation suite on a small random Pix2Struct built offline from the committed config and tokenizer, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/pix2struct_textcaps_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4). The fine-tuning recipe has not been run on the real checkpoint, so the notebook records whether the adapted model beats the frozen one instead of asserting it. The Kaggle and local runs in `docs/release-verification.md` were of the previous `TASK-INFERENCE` notebook and are not evidence for this revision; the repository is not release-grade until a Colab/Kaggle execution for the exact candidate blob is recorded there.
