# Release status

Current status: **Release-grade** — the `E2E` tutorial notebook `tutorials/pix2struct_textcaps_colab.ipynb` (blob `2b72c3f7`, committed at `7eb0691`) executed top-to-bottom in a clean Kaggle Tesla T4 runtime on 2026-09-24 (11/11 code cells ok, 1 restart after the install cell, 902.2 s, 355 files / 1218 MB staged from the Hub into an empty cache), recorded in `docs/release-verification.md`; held-out test CIDEr-D frozen 0.309 → adapted 0.325 (`text` 0.490 → 0.504, `no-text` 0.068 → 0.086; constant-caption baseline 0.052), best validation epoch 4 of 4; the exported adapter reloaded with 8/8 identical captions. The fine-tuning recipe (`LEARNING_RATE = 1e-5`, four epochs, two decoder blocks, batch 8) was measured on the same T4 against 2e-5 and 5e-5 before promotion (see the pre-flight row); every recipe beat the frozen model, but by margins (+0.012 to +0.031 CIDEr-D on 70 photographs, one seed) too small to assert, so the notebook records `adapted_beats_frozen` rather than asserting it. At that revision the pipeline package with its adaptation contract, the offline and model-backed suites (the real-checkpoint and CUDA cases pass on the T4), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), the static validator, the generator parity checks and the CI workflow exist and are green. Any later change to the carried modules or to the notebook yields a new blob and returns the registry to **Candidate** until a clean run of that blob is recorded.
